const fs = require('fs');
const path = require('path');
// if you want to run your whole project just by executing index.js file, uncomment the following line
// const crud = require('./fileSystem/crudUsingFS.js');
const dir = path.join(__dirname, 'data');

console.log(dir);
console.log("INDEX JS!");